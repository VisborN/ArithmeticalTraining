apply plugin: 'com.android.application'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.3"

    defaultConfig {
        applicationId "com.dudkovlad.arithmetical_training"
        minSdkVersion 16
        targetSdkVersion 23
        versionCode 5
        versionName "2.3"
    }
    signingConfigs {
        debug {
        }

        myConfig {
            storeFile file("C:\\KeyStore_.jks")
            storePassword "Fkjycbfkjycj16k"
            keyAlias "Arithmetical training"
            keyPassword "Fkjycbfkjycj16a"
        }
    }

    buildTypes {

        debug {
            applicationIdSuffix ".debug"
        }

        release {
            minifyEnabled false
            debuggable false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.myConfig
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:23.3.0'
    compile 'com.android.support:support-v4:23.3.0'
    compile 'com.yandex.android:mobmetricalib:2.40'
}
